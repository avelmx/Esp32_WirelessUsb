Wireless usb connection emulation using esp32
=====================================

This a project was built using platformio  and uses custom library to emulate a usb host functionality via gpio pins

```